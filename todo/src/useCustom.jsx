import React, { useEffect, useState } from "react";

export default function useCustom(defaultvalue) {
  cosnt[(value, setValue)] = useState(defaultvalue || "");

  useEffect(() => {
    setValue("new Value");
  }, []);

  return value;
}
